name: vitalmap
description: VitalMap - organ health risk indicator (Flutter frontend)
publish_to: 'none'
version: 0.1.0
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  shared_preferences: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.10.0
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/logo_medid.jpeg

# Configuration for flutter_launcher_icons
flutter_icons:
  android: true
  ios: false
  web:
    generate: true
    image_path: "assets/app_icon_legacy.png"
    background_color: "#F4FBFF"
    theme_color: "#4BAFE3"
  image_path: "assets/app_icon_legacy.png"
  adaptive_icon_background: "#F4FBFF"
  adaptive_icon_foreground: "assets/app_icon_foreground.png"
